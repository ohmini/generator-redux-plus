// Auto-generated by the generator
// Don't manually edit this file

<% for (sl in selectors) {
  const {name} = selectors[sl]
-%>
import {<%= name %>} from './<%= name %>'
<% } -%>

export const <%= state %>Selectors = {
<% for (sl in selectors) {
  const {name} = selectors[sl]
-%>
  <%= name %>,
<% } -%>
}
