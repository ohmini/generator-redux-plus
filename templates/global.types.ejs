// Auto-generated by the generator
// Don't manually edit this file

// @flow

<% for (s in states) { -%>
import type {<%= states[s].flowType %>} from './<%= states[s].name %>/types'
<% } -%>

export type ApplicationState = {
<% for (s in states) { -%>
  <%= states[s].name %>: <%= states[s].flowType %>,
<% } -%>
}
